A website that tracks covid cases and shows total cases for each country with the ability to sort/filter said countries.

Install packages by running
`npm install`

Then run the project using
`npm start`
